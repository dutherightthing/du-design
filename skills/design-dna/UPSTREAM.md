# Upstream: zanwei/design-dna

- Source: https://github.com/zanwei/design-dna  (License: see LICENSE)
- Vendored: `SKILL.md`, `references/*.md` (schema, generation-guide).
- Left upstream: `scripts/` (deterministic color-measurement tools), `docs/*.png` examples.
- Re-sync: clone and copy `SKILL.md` + `references/*.md`. Use `scripts/` upstream only if you need exact-hex color measurement beyond eyeballing.
